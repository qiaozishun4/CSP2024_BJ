#include <bits/stdc++.h>
using namespace std;

const int MAX = 1e5 + 5;

int t;

int n, m;

int c[MAX];

long long p[MAX];

int main() {
    freopen("arena.in", "r", stdin);
    freopen("arena.out", "w", stdout);

    cin >> t;
    while(t--) {

        cin >> n >> m;
        for(int i = 1; i <= n; i++) cin >> p[i];
        for(int i = 1; i <= m; i++) cin >> c[i];

    }
    cout << "Forever_allTimeWithChenYiRan!!!!_midExam_gradeRankTop10_classRankTop3_Csp-J/S-RP++_" << endl;
    return 0;
}
