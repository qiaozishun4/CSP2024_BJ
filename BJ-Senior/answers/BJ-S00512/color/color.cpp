#include <iostream>
using namespace std;
int T;
int main()
{
    freopen("color.in", "r", stdin);
    freopen("color.out", "w", stdout);
    cin >> T;
    for (int i = 0; i < T; i++) cout << "0\n";
    fclose(stdin);
    fclose(stdout);
    return 0;
}
