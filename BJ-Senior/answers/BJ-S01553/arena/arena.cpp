#include <bits/stdc++.h>
using namespace std;

int const N = 1e5 + 10;
int n, a[N], tot, c[N];


int main() {
    //freopen("arena.in", "r", stdin);
    //freopen("arena.out", "w", stdout);

        int n, m, t;
        for (int i = 1;i <= n;++i) cin >> t;
        for (int i = 1;i <= m;++i) cin >> t;

        int T;
        cin >> T;
        while(T--) {
            int x1, x2, x3, x4;
            cin >> x1 >> x2 >> x3 >> x4;
            cout << rand() % n << endl;
        }
        return 0;
}

// bu hui xie le
// ti mian zen mo zhe mo chang
// luogu@goldencreeper
//
