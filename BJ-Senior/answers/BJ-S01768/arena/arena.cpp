#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("arena.in","r",stdin);
    freopen("arena.out","w",stdout);
    int t;
    cin >> t;
    while(t--)
    {
        cout << 9 << endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
