#include<bits/stdc++.h>
using namespace std;

const int maxn = 1e5;
int t,n,m,l,v,d[maxn],u[maxn],a[maxn],p[maxn];
struct fenshu{
    int fenmu;
    int fenzi;
};

int main()
{
    freopen("detect.in", "r",stdin );
    freopen("detect.out","w",stdout);
    cin >> t;
    for(int q = 0;q < t;q++)
    {
        scanf("%d %d %d %d ",&n,&m,&l,&v);
        for(int i = 0;i < n;i++)
        {
            scanf("%d %d %d ",&d[i],&u[i],&a[i]);
        }
        for(int i = 0;i < m;i++)
        {
            scanf("%d ",&p[i]);
        }
        ////////////////////////////////////////////////

    }







    return 0;
}
