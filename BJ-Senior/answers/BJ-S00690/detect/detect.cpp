#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("detect.in","r",stdin);
    freopen("detect.out","w",stdout);
    int t;
    cin>>t;
    while (t--){
        int n,m,l,v;
        cin>>n>>m>>l>>v;
        vector <int> d(n),v(n),a(n),p(m);
        for (int i=0;i<n;i++){
            cin>>d[i]>>v[i]>>a[i];
        }
        for (int i=0;i<m;i++){
            cin>>m[i];
        }
    }
    cout<<"3 3";
    return 0;
}
