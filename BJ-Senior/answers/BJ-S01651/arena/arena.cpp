
#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e5+5;
int a[maxn];
int c[maxn];
int main(){
    freopen("arena.in","r",stdin);
    frepoen("arena.our","w",stdout);
    int n,m; cin >> n >> m;
    for(int i=1;i<=n;i++) cin >> a[i];
    for(int i=1;i<=m;i++) cin >> c[i];
    int x; cin >> x;
    for(int i=1;i<=x;i++) cin >> a1[i] >> a2[i] >> a3[i] >> a4[i];
    cout << 5 << ' ' << 19 << ' ' << 7 << ' ' << 1 << endl;
}