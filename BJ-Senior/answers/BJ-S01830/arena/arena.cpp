#include <bits/stdc++.h>
using namespace std;
int a[1000], c[1000];

int main(){
    freopen("arena.in", "r", stdin);
    freopen("arena.out", "w", stdout);
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++){
        cin >> a[i];
    }
    for (int i = 0; i < m; i++){
        cin >> c[i];
    }
    
    return 0;
}
