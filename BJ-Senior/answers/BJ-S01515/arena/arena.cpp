#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("arena.in","r",stdin);
    freopen("arena.out","w",stdout);
    int t;
    cin>>t;
    while(t--)//I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!I CAN NOT DO THAT!!!NNNNNNNOOOOOOOOOO
        {
        int n,m;
        cin>>n>>m;
        int a[100005],c[100005];
        for(int i=1;i<=n;i++)
            cin>>a[i];
        for(int i=1;i<=m;i++)
            cin>>c[i];
        int k=0,text=1;
        while(text<n){
            text*=2;
            k++;
        }
        for(int i=1;i<=k;i++)
        {
            cout<<"I CAN NOT DO THAT!!!"<<endl;
        }

    }
    return 0;
}
