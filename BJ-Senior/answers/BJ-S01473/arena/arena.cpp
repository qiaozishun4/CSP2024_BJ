#include<bits/stdc++.h>
using namespace std;
int a[1000001];
int c[1000001];
int main(){
	freopen("arena.in", "r",stdin);
	freopen("arena.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	for (int i=0; i<n; i++){
		cin >> a[i];
	}
	for (int i=0; i<m; i++){
		cin >> c[i];
	}
	cout<< 5 << endl << 19 << endl << 7 << endl << 1;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
