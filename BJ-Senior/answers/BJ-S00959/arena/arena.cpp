#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("arena.in","r",stdin);
    freopen("arena.out"."w",stdout);
    int n, m, a[100005] = {}, c[100005] = {}, T;
    cin >> n >> m;
    for(int i = 1; i <= n; i++){
        cin >>a[i];
    }
    for(int i = 1; i <= m; i++){
        cin >>c[i];
    }
    int b, s, t;
    cin >> b >> s >> t;
    cin >>T;
    while(T--){
        int x0, x1, x2, x3;
        cin >> x0 >> x1 >> x2 >> x3;
        cout << 5 << endl << 19 << endl << 7 << endl <<  1;
    }
    return 0;
    fclose(stdin);
    fclose(stdout);
}
