#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll a[15],c[15];
bool f[5][20];
int n,m,T,X[8];
int main()
{
    freopen("arena.in","r",stdin);
    freopen("arena.out","w",stdout);
    printf("5");
    /*
    scanf("%d%d",&n,&m);
    for(int i=1;i<=n;++i)
        scanf("%lld",&a[i]);
    for(int i=1;i<=m;++i)
        scanf("%lld",&c[i]);
    int h;
    if(n==1)
        h=0;
    else
    for(int i=0;i<=2;++i)
        if((1<<i)<n&&(1<<(i+1))>=n)
            h=i+1;
    for(int i=1;i<=h;++i)
    {
        for(int j=0;j<(1<<(h-i));++j)
            scanf("%d",&f[i][j]);
    }
    scanf("%d",&T);
    for(int i=1;i<=4;++i)
    {
        scanf("%d",&X[i]);
    }
    for(int i=1;i<=n;++i)
    {
        a[i]^=X[i%4+1];
    }
    for(int i=1;i<=h;++i)
    {

    }*/
    return 0;
}
