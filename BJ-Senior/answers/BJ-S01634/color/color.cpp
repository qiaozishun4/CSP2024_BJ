#include<bits/stdc++.h>
using namespace std;
int n;
int b;
int a[100005];
int main()
{   freopen("color.in","r",stdin);
    freopen("color.out","w",stdout);
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>b;
        for(int j=0;j<=b;j++)
        {
            cin>>a[i];
        }
    }
    for(int i=0;i<n;i++)
    {
        
        cout<<0<<endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;

}
