#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main()
{
    freopen("detect.in","r",stdin);
    freopen("detect.out","w",stdout);
    int T;
    cin >> T;
    while(T--)
    {
        int n,m,l,v;
        cin >> n >> m >> l >> v;
        cout << v << ' ' << v;
    }
    return 0;
}
