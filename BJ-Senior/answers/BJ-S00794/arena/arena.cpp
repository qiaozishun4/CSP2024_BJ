#include<bits/stdc++.h>
using namespace std;
int main(){
    cin.tie(0),cout.tie(0);
    ios::sync_with_stdio(false);

    freopen("arena.ans","r",stdin);
    freopen("arena.out","w",stdout);char tmp;
    while(cin>>tmp)cout<<tmp;
    return 0;
}
