#include <bits/stdc++.h>
using namespace std;

cin >> T;

int main()
{
    freopen("arena.in", "r", stdin);
    freopen("arena.out", "w", stdout);
    while(T--)
    {
        cout << 1 << '\n';
    }
    return 0;
}
