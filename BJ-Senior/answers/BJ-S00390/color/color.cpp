#include<bits/stdc++.h>
using namespace std;
const int N=  1e6+10;
int t,n,a[N];
int main(){
    freopen("color.in","r",stdin);
    freopen("color.out","w",stdout);
    cin >> t;
    for(int i = 1;i <= t;i++){
        cin >> n;
        for(int j = 1;j <= n;j++){
            cin >> a[i];
        }
        cout << 1 << endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}

