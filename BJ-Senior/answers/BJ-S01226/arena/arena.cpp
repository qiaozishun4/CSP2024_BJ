#include<bits/stdc++.h>
#define ll long long
using namespace std;
ll a[100005],mp[100005],b[100005],ans,cnt,t;
int cmp(ll x,ll y)
{
    return x>y;
}
int main()
{
	ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
	freopen("arena.in","r",stdin);
	freopen("arena.out","w",stdout);
	cin>>t;
	while(t--)
	{
		cout<<0<<'\n;
	}
	return 0;
}
