#include <bits/stdc++.h>
using namespace std ;

int main () {
    freopen ( "arena.in" , "r" , stdin ) ;
    freopen ( "arena.out" , "w" , stdout ) ;
    int T ;
    cin >> T ;
    for ( int i = 1 ; i <= T; ++ i ) {
        cout << 0 << endl ;
    }
    return 0 ;
}
