#include<bits/stdc++.h>
using namespace std;
/*int n, m;
int A[150005], a[150005];
int c[150005];
int d[150005];
int ans[150005];
int ansnow[150005];
int T, X[10];
int solve(int l, int r, int k, int aa, int R){
    if(l == r){
        if(aa <= l){
            ansnow[l] = a[l];
            return l;
        }
        else{
            ansnow[l] = -1;
            return -1;
        }
    }
    int lwin = solve(l, (l + r) / 2, k * 2, aa, R - 1), rwin = solve((l + r) / 2 + 1, r, k * 2 + 1, R - 1);
    if(lwin != -1 && rin != -1){
        if(d[k]){
            if(a[rwin] >= R)
                ansnow[]
        }
    }
}*/
signed main(){
    /*freopen("arena.in", "r", stdin);
    freopen("arena.out", "w", stdout);
    cin>>n>>m;
    int k = 0;
    while((1 << k) < n)
        k++;
    for(int i = 0;i <= k - 1;i++){
        for(int j = 0;j < (1 << i);j++)
            cin>>d[(1 << i) + j];
    }
    cin>>T;
    while(T--){
        for(int i = 0;i < (1 << k);i++)
            ans[i] = 0;
        cin>>X[0]>>X[1]>>X[2]>>X[3];
        for(int i = 1;i <= n;i++)
            a[i] = (A[i] ^ X[i % 4]);
        ans[1] = 1;
        for(int i = 1;i < k;i++){
            for(int j = 0;j < (1 << k);j++)
                ansnow[j] = 0;
            for(int j = 0;j < (1 << i);j++)
            int s = solve(0, (1 << i) - 1, (1 << (k - i - 1)), (1 << (i - 1)) + j, i);
        }
        for(int j = 0;j < (1 << (k - 1));j++){
            for(int i = 0;i < (1 << k);i++)
                ansnow[i] = 0;
            int s = solve(0, (1 << k), 0, (1 << (k - 1)) + j, k - 1);
        }
    }*/
    return 0;
}
