#include <bits/stdc++.h>
/*with xi forever*/
using namespace std;
const int MAXN = 100005;

int T;
int n, a;

int main() {
    freopen("color.in", "r", stdin);
    freopen("color.out", "w", stdout);
    cin >> T;
    for (int i = 1; i <= T; ++i) {
        cin >> n;
        for (int j = 1; j <= n; ++j) {
            cin >> a;
        }
        cout << 0 << endl;
    }
    return 0;
}
