#include <iostream>
#include <cstdio>
using namespace std;
const int N=1e5;
int n,m,a[N],c[N];
int main(){
    freopen("arena.in","r",stdin);
    freopen("arena.out","w",stdout);

    cout<<1;

    fclose(stdin);
    fclose(stdout);
    return 0;
}