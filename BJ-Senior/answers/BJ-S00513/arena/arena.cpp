#include <iostream>
#include <stdio.h>
using namespace std;

int main()
{
    freopen("arena.in", "r", stdin);
    freopen("arena.out", "r", stdout);
    int n;
    cin >> n;
    cout << n;
    return 0;
}