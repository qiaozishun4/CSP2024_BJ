#include <iostream>
#include <algorithm>

using namespace std;
typedef long long ll;

const int N = 2 * 1e5;
ll T, a[N], b[N];

int main()
{
    cin >> T;
    while(T--)
    {
        ll n, m, l, v;
        cin >> n >> m >> l >> v;
        for (int i = 0, c, d, e; i < n; i++)
        {
            cin >> c >> d >> e;
        }
        for (int i = 0; i < m; i++)
        {
            cin >> b[i];
        }
    }
    cout << 3 << "\n" << 3;
    return 0;
}
