#include<bits/stdc++.h>
using namespace std;
int t, n, m;
int main(){
    freopen("detect.in", "r", stdin);
    freopen("detect.out", "w", stdout);
    cin >> t;
    for(int i = 1; i <= t; i++){
        cin >> n >> m;
        cout << n << " " << 0 << endl;
    }
    return 0;
}
