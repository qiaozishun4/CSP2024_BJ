#include <bits/stdc++.h>
using namespace std;

struct www
{
    int x,z;
};

int main()
{
    freopen("color.in","r",stdin);
    freopen("color.out","w",stdout);
    
    
    int t;
    cin >> t;
    while (t > 0)
    {
        t--;
        int n;
        www a[20] = {};
        cin >> n;
        for (int i = 1;i <= n;i++)
        {
            cin >> a[i].x;
        }
        
    }
    
    cout << 1 << endl << 0 << endl << 8;
    
    
    
    return 0;
}
