#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("arena.in","r",stdin);
    freopen("arena.out","w",stdout);
    int n,m,a[100005],b[100005];
    cin >> n >> m;
    for (int i=1;i<=m;i++)
    {
        
        cout << m/2+1 << "\n";
    }
    return 0;
}

