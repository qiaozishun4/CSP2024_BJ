#include <bits/stdc++.h>
using namespace std;
void calc()
{
    int n, m, l ,v;
    cin >>n >>m >>l >>v;
    cout << v <<" "<< v << endl;
}

int main()
{
    freopen("detect.in", "r", stdin);
    freopen("detect.out", "w", stdout);
    int t;
    cin >> t;
    while(t--)
    {
        calc();
    }
    return 0;
}
