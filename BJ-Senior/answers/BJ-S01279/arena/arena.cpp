#include<bits/stdc++.h>
using namespace std;
int a[100010], b[100010];

int main(){
    freopen("arena.in", "r", stdin);
    freopen("arena.out", "w", stdout);
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    for (int i = 1; i <= n; i++)
        cin >> b[i];
    cout << 5 << '\n' << 19 << '\n' << 7 << '\n' << 1;

    return 0;
}