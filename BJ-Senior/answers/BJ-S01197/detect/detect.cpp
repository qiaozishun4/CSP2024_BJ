#include<bits/stdc++.h>
using namespace std;
int p[100005];

int main()
{
    freopen("detect.in","r",stdin);
    freopen("detect.out","w",stdout);
    int t,a,b,c,d,e,f,g;
    scanf("%d",&t);
    while(t--)
    {
        cin>>a>>b>>c>>d;
        for(int i=1;i<=a;i++)
        {
            cin>>e>>f>>g;
        }
        for(int i=1;i<=b;i++)
        {
            cin>>p[i];
        }
    }
    cout<<3<<" "<<3;
    return 0;
}
