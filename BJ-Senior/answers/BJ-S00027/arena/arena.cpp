#include <iostream>
#include <algorithm>
#include <cstdlib>
#include <cstdint>
#include <bits/stdc++.h>
#include <set>

using namespace std;

const int maxn = 15, maxm = 2 * 1e5 + 5;
int n[maxn], a[maxn][maxm];

int main() {
    freopen("arena.in", "r", stdin);
    freopen("arena.out", "w", stdout);

    time_t *timer = new time_t;
    uint64_t rand = time(timer), rand2 = time(timer), rand3 = time(timer), rand4 = time(timer);
    
    printf("%d%d%d%d", rand, rand2, rand3, rand4);
    return 0;
}
