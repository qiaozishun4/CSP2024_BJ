#include<bits/stdc++.h>
using namespace std;
int main(void)
{
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    srand(time(0));
    int T;
    for(cin>>T;T;T--)
    {
        int n,k,q;
        for(int i=0;i<n;i++)
        {
            int l,sb;
            while(l--)cin>>sb;
        }
        for(int i=0;i<q;i++)
        {
            int a;
            cin>>a>>a;
        }
        while(q--)cout<<(rand()&1)<<endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
