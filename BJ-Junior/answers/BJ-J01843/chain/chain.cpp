#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int n;
    cin >> n;
    while(n--) {
        int u, v, w;
        cin >> u >>v >> w;
        for(int i = 0; i < w; i++) {
            cout << 0 << "\n";
        }
        cout << "\n";
    }
    return 0;
}
