#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("chain1.in","r",stdin);
    freopen("chain.out","w",stdout);
    //next_permutation();
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int t;
    cin>>t;
    while(t--){
        int n,k,q;
        cin>>n>>k>>q;
        for(int i=1;i<=n;i++){
            int l;
            cin>>l;
            for(int j=1;j<=l;j++){
                string a;
                cin>>a;
            }

        }
        for(int i=1;i<=q;i++){
            int r,c;
            cin>>r>>c;
            cout<<0<<endl;
        }
    }
    return 0;
}
