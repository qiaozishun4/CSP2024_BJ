#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int T, n, k, q;

int main()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int temp = 0;
    int s;
    cin >> T;
    for (int z = 0; z < T; z++)
    {

        cin >> n >> k >> q;
        for (int i = 0; i < q; i++)
        {
            cout << 0 << endl;
        }
    }
    return 0;
}
