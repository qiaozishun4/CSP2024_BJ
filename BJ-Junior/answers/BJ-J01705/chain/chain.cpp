#include <bits/stdc++.h>
using namespace std;
int s[200010];
int main()
{
    freopen("chain.in" , "r" , stdin);
    freopen("chain.out" , "w" , stdout);
    int t , n , k , q;
    cin >> t;
    while(t--)
    {
        cin >> n >> k >> q;
        int l;
        cin >> l;
        for(int i = 1; i <= l; i++)
        {
            cin >> s[i];
        }
        while(q--)
        {
            int r , c;
            cin >> r >> c;

        }
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
