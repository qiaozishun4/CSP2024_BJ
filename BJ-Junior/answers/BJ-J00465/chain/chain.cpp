#include<iostream>
using namespace std;
int main()
{
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    int t;
    cin>>t;
    while(t--)
    {
        int n,m,k,x,y,q,ans=1;
        cin>>n>>k>>q;
        for(int i=1;i<=n;i++)
        {
            int l,s;
            cin>>l;
            while(l--) cin>>s;
        }
        while(q--)
        {
            int r,c;
            cin>>r>>c;
        }
        cout<<1<<'\n';
    }
}
