#include<iostream>
#include<cstdio>
#include<algorithm>
using namespace std;
typedef long long LL;
LL T;
int main(){
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    cin >> T;
    for(LL i = 1; i <= T; i++){
        LL n,k,q;
        cin >> n >> k >> q;
        for(LL j = 1; j <= n; j++){
            LL l;
            cin >> l;
            for(int k = 1; k <= l; k++){
                LL s;
                cin >> s
            }
        }
        for(int j = 1; j <= q; j++){
            int r,c;
            cin >> r >> c;
        }
    }
    for(int i = 1; i <= q; i++)
        cout << 1 << endl;
    return 0;
}
