#include <bits/stdc++.h>
using namespace std;

int t;

int n, k, q;

int main() {
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    
    cin >> t;
    
    while(t--) {
        cout << 0 << endl;
    }

    return 0;
}