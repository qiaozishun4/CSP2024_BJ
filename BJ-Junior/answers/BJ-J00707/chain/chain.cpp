#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    int T;
    cin>>T;
    while(T--)
    {
        int n,k,q;
        cin>>n>>k>>q;
        while(n--)
        {
            string s;
            getline(cin,s);
        }
        while(q--)
        {
            string s;
            getline(cin,s);
            cout<<0<<endl;
        }
    }
    return 0;
}