#include <bits/stdc++.h>
using namespace std;
int t,n,k,q;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    cin>>t;
    while(t--)
    {
        cin>>n>>k>>q;
        for(int i=0;i<1;i++) cout<<0<<endl;
    }
    return 0;
}
