#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    int T;
    cin >> T;
    while(T--)
    {
        int n,a;
        cin >> n;
        cin >> a;
        cin >> a;
        while(a--)
        {
        cout << 0 << endl;
        }
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
