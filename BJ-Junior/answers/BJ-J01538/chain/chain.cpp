#include <bits/stdc++.h>

using namespace std;

int t;
long long n, k, q, l, s;
int main()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    
    cin >> t;
    for (int i = 0; i < t; i++) {
        cin >> n >> k >> q;
        for (int j = 0; j < n; j++) {
            cin >> l >> s'
        }
    }
    
    cout << "1\n 0\n 1\n 0\n 1\n 0\n 0" << endl
    
    fclose(stdin);
    fclose(stdout);
    return 0;
}