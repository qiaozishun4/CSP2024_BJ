#include <iostream>

using namespace std;

int main()
{
    int T;
    cin >>T;
    while(T--)
    {
        int n, p, q;
        cin >> n >> p >> q;
        for (int i = 0; i <n; i++)
        {
            int l;
            cin >> l;
            for (int i = 0; i < l; i++)
            {
                int a;
                cin >> a;
            }
            for (int i = 0; i < l; i++)
            {
                int a;
                cin >> a;
                cout << 1 << "\n";
            }
        }
    }
    return 0;
}
