# include <bits/stdc++.h>
using namespace std;

int n, k, q;

int main()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int t;
    cin >> t;
    while (t--)
    {
        cin >> n >> k >> q;
        for (int i = 1; i <= n; i++)
        {
            int l;
            cin >> l;
            while (l--)
            {
                int y;
                cin >> y;
            }
        }
        for (int i = 1; i <= q; i++)
        {
            int r, c;
            cin >> r >> c;
        }
        while (q--)
            cout << 1 << endl;
    }
    return 0;
}
