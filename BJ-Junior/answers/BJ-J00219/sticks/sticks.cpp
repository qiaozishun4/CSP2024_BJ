#include <bits/stdc++.h>
using namespace std;
int c[5][20];
int main() {
    freopen("sticks.in", "r", stdin);
    freopen("sticks.out", "w", stdout);
    int Q; cin >> Q;
    while (Q--) {
        int n; cin >> n;
        cout << 10;
        for (int i = 1; i <= (n - 8) / 7; ++i) cout << 8;
        cout << endl;
    }
}
