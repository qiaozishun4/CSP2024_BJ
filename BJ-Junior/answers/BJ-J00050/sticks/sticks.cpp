#include<bits/stdc++.h>
using namespace std;
int a[51];
int b[51];
int main(){
    freopen("sticks.in","r",stdin);
    freopen("sticks.out","w",stdout);
    int T;
    scanf("%d",&T);
    for(int i=1;i<=T;i++){
        scanf("%d",&a[i]);
    }
    for(int i=1;i<=n;i++){
        if
        if(a[i]==2) b[i]=1;
        if(a[i]==3) b[i]=7;
        if(a[i]==4) b[i]=4;
        if(a[i]==5) b[i]=2;
        if(a[i]==6) b[i]=0;
        if(a[i]==7) b[i]=8;
    }
    for(int i=1;i<=n;i++){
        printf("%d",&b[i])
    }
    return 0;
}
