#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("stick.in","r",stdin);
    freopen("stick.out","w",strout);
    int n;
    cin>>n;
    while(n--) cout<<-1;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
