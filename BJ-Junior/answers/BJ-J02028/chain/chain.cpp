#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("chain.in","r",stdin);
    freopen("chain.out","w", stdout);
    int T;
    cin >>T;
    while(T--){
        int n, k, q;
        cin >>n >>k >> q;
        while(n--){
            int l, x;
            cin >> l;
            for(int i = 1; i <= l; i++) cin >> x;
        }while(q--){
            int l, r;
            cin >> l >> r;
            cout << 0 << '\n';
        }
    }
    return 0;
}
