#include <bits/stdc++.h>
using namespace std;

int t, n;

int main()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    cin >> t;
    while (t--)
    {
        cin >> n;
        cout << 0 << endl;
    }
    return 0;
}

