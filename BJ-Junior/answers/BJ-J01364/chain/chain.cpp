#include<bits/stdc++.h>
using namespace std;

struct num{
    int h, w[10005], i = 0;
};

int main(){
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    cout<<0<<endl;

    fclose(stdin);
    fclose(stdout);
    return 0;
}
