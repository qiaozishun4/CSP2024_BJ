#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    int t;
    cin>>t;
    while(t--){
        int n,k,q,T;
        cin>>n>>k>>q;
        while(n--){
            cin>>T;
            int T2;
            while(T--)cin>>T2;
        }
        for(int i=0;i<q;++i)cin>>n>>k;
        while(q--)cout<<"0\n";
    }
    return 0;
}
//真是的呢 到了最后一刻还没入门