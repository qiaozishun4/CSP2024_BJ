#include <bits/stdc++.h>
using namespace std;
int t[2000005], sum;
int main()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    while(cin >> t[sum])
    {
        sum++;
    }
    for (int i = 1; i <= t[1]; i++)
    {
        for (int j = 1; j <= t[4]; j++)
        {
            cout << 1 << endl << 0;
        }
    }
    return 0;
}
