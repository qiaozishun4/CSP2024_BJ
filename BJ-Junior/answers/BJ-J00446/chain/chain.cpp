#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("chain.in", "r", stdin);
	freopen("chain.out", "w", stdout);
	int T;
	cin >> T;
	while(T--){
		int n, k, q;
		cin >> n >> k >> q;
		while(n--){
			int x;
			cin >> x;
			while(x--){
				int tmp;
				cin >> tmp;
			}
		}
		while(q--){
			int r, c;
			cin >> r >> c;
		}
	}
	cout << "1\n0\n0\n1\n0\n1\n0";
}
