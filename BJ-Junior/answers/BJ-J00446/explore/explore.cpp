#include<bits/stdc++.h>
using namespace std;
int T;
int main(){
	freopen("explore.in", "r", stdin);
	freopen("explore.out", "w", stdout);
	cin >> T;
	while(T--){
		string s;
		int n, m, k, x, y, d;
		cin >> n >> m >> k >> x >> y >> d;
		while(n--)cin >> s;
		cout << k << '\n';
	}
	return 0;
}
