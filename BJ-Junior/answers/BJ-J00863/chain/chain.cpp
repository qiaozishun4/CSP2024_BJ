#include<bits/stdc++.h>
using namespace std;

int T;
int n;

int main(){
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);

    cin >> T;

    while(T--){
        int n, k, q;
        cin >> n >> k >>q;
        for(int i=1; i<=q; i++){
            cout << 0 << endl;
        }
    }

    return 0;
}
