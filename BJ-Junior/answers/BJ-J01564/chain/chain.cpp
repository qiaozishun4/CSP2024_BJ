#include<iostream>
#include<set>
#define ll long long
using namespace std;
ll T, q;
string x;
set <string> s;
int main(){
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    cin >> T;
    while(T--){
        cin >> q >> q >> q;
        while(q--){
            cout << 0 << endl;
        }
    }
    return 0;
}
