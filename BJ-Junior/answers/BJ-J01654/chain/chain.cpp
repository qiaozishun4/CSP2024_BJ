#include <bits/stdc++.h>
using namespace std;
int t,n,k,q,l,s,x,y;
int main()
{
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    cin>>t;
    while(t--){
    cin>>n>>k>>q;
    while(n--)
    {
        cin>>l;
        while(l--) cin>>s;
    }
    cin>>q;
    while(q--)
    {
        cin>>x>>y;
        cout<<0<<endl;
    }
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
