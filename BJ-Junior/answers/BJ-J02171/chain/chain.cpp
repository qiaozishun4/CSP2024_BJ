#include<bits/stdc++.h>
using namespace std;
int t, n, k, q;
int main(){
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);

    cin >> t;
    while(t--){
        cin >> n >> k >> q;
        while(q--){
            cout << 0 << endl;
        }
    }

    fclose(stdin);
    fclose(stdout);

    return 0;
}
