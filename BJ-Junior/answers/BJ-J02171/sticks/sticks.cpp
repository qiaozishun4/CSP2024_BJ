#include<bits/stdc++.h>
using namespace std;

int main(){
    freopen("sticks.in", "r", stdin);
    freopen("sticks.out", "w", stdout);
    cout << "-1";

    fclose(stdin);
    fclose(stdout);

    return 0;
}
