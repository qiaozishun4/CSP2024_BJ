#include<iostream>
using namespace std;

int n = 0, num, l[52] = {};
char a,b;

int main(){
    freopen("poker.in","r",poker.in);
    freopen("poker.out","w",poker.out);

    cin >> n;
    for(int i = 0;i < n; i++){
        cin >> num;
        cin >> a >> b;
    }
    cout << (52-n);
    fclose(poker.in);
    fclose(poker.out);
    return 0;
}