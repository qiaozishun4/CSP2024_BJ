#include<iostream>
using namespace std;

int main(){
    fopen("sticks.in","r",sticks.in);
    fopen("sticks.out","w",sticks.out);
    cout << -1;
    fclose(sticks.in);
    fclose(sticks.out);
    return 0;
}