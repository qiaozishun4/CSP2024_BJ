// 勤奋 求实 创造 奉献
// Luogu Coder12823
// 塞尔达是天

#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int T;
    scanf("%d", &T);
    while (T--) {
        int n, k, q;
        scanf("%d%d%d", &n, &k, &q);
        for (int i = 1; i <= n; i++) {
            int l;
            scanf("%d", &l);
            for (int j = 1; j <= l; j++) {
                int s;
                scanf("%d", &s);
            }
        }
        for (int i = 1; i <= q; i++) {
            int r, c;
            scanf("%d%d", &r, &c);
        }
        for (int i = 1; i <= q; i++) {
            printf("0\n");
            // [CSP-S 2022] [星战] 不可以，总司令
        }
    }

    return 0;
}

// C C F 快让我得 1=