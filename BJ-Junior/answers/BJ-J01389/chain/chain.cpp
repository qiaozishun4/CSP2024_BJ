#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("chain.in","r",stdin);
    freopen("chain.out","w",stdout);
    int q;
    cin>>q;
    while(q--)
    {
        int n,k,q;
        cin>>n>>k>>q;
        int l;
        for(int i=1;i<=n;i++)
        {
            cin>>l;
            for(int j=1;j<=l;j++)
            {
                int s;
                cin>>s;
            }
        }
        while(q--)
        {
            int r,c;
            cin>>r>>c;
            cout<<1<<endl;
        }
    }
    return 0;
}
