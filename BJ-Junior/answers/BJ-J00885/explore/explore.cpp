include<iostude>
using int min()
{
 for{
      int[n<=(x,y)];
      int[1< i < n,1 < j < m];
      int[x=x+1y ,d=2];
      int[y=x-1y,d=0];
      cout << (x,y);
      cin >> (x,y);
      }
return 0;
}
  
