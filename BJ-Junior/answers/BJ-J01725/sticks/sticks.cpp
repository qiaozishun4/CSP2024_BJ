#include<bits/stdc++.h>
using namespace std;

int main() {
    freopen("sticks.in", "r", stdin);
    freopen("sticks.out", "w", stdout);
    int n, i;
    cin >> n;
    for(i = 1; i <= n; i++) {
        int x;
        cin >> x;
        cout << -1;
    }
    return 0;
}
