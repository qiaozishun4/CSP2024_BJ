#include<bits/stdc++.h>
using namespace std;

int main() {
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int n, i;
    cin >> n;
    for(i = 1; i <= n; i++) {
        int n_, k, q;
        cin >> n_ >> k >> q;
        int i_1, i_2;
        for(i_1 = 1; i_1 <= n_; i_1++) {
            int l;
            cin >> l;
            for(i_2 = 1; i_2 <= l; i_2++) {
                int s;
                cin >> s;
            }
        }
        for(i_1 = 1; i_1 <= q; i_1++) {
            int r, c;
            cin >> r >> c;
            cout << 0 << endl;
        }
    }
    return 0;
}
