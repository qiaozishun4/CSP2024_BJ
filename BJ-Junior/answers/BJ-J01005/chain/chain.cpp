#include <iostream>
#include <cstdio>
using namespace std;
int main() {
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    for (int i = 0; i < c; ++ i) cout << 0 << endl;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
