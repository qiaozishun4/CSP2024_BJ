#include <bits/stdc++.h>
using namespace std;

int main ()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);
    int T;
    cin >> T;
    while (T--)
    {
        int n, k, q;
        cin >> n >> k >> q;
        for (int i = 1; i <= n; i++)
        {
            int a;
            cin >> a;
            for (int j = 1; j <= a; j++)
            {
                int b;
                cin >> b;
            }
        }
        while (q--)
        {
            int a, b;
            cin >> a >> b;
            cout << 0 << endl;
        }
    }
    return 0;
}
