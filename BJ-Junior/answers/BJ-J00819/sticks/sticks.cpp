#include <bits/stdc++.h>
int main(){
    freopen("sticks.in","r",stdin);
    freopen("sticks.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long long a,n;
    cin>>a;
    while(a){
        cin>>n;
        cout<<-1<<endl;
        a--;
    }
    return 0;
}
