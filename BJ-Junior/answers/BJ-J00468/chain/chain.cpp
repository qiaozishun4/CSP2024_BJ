#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);

    int t;
    cin >> t;

    while(t--) {
        int n, k, q;
        cin >> n >> k >> q;
        int l, temp;
        while(n--) {
            cin >> l;
            while(l--) {
                cin >> temp;
            }
        }
        while(q--) {
            cin >> l  >> temp;
            cout << 1 << endl;
        }
    }

    return 0;
}
