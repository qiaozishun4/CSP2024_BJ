#include <iostream>
using namespace std;
int main() {
	freopen("chain.in", "r", stdin);
	freopen("chain.out", "w", stdout); 
	int t;
	cin >> t;
	int n, k, q;
	for (int i = 0; i < t; i++) {
		cin >> n >> k >> q;
		for (int j = 0; j < q; j++) {
			cout << 1 << endl;
		}
	}
	return 0;
}

