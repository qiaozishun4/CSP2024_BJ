#include <bits/stdc++.h>
using namespace std;
int t;
int main() {
freopen("chain.in", "r", stdin);
freopen("chain.out", "w", stdout);
cin >> t;
while (t--) {
int n, k, q;
cin >> n >> k >> q;
for (int i = 1; i <= n; i++) {int l; cin >> l; while (l--) {int x; cin >>x;}}
while (q--) {int l, r;cin>>l >> r;}
while (n--) cout << 0 << endl;
}
return 0;
}