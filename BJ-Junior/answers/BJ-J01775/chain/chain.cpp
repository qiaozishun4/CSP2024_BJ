#include <iostream>

using namespace std;

int main ()
{
    freopen("chain.ans", "r", stdin)'
    freopen("chain.out", "w", stdout);
    int f;
    while (cin >> f)
    {
        cout << f << endl;
    }
    return 0;
}