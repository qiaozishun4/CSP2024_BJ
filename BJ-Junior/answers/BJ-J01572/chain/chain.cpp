#include<iostream>
using namespace std;

int n;

int main()
{
    freopen("chain.in", "r", stdin);
    freopen("chain.out", "w", stdout);

    ios::sync_with_stdio(false);

    for(int i = 1; i <= 5000; i ++)
    {
        cout << 1 << endl;
    }

    return 0;
}